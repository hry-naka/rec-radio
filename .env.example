# NHK API Configuration
# Get your API key from https://api.nhk.or.jp/
NHK_API_KEY=your_api_key_here

# Location and Area Configuration
# Location: Where you are listening (e.g., tokyo, osaka, hokkaido)
# Area Code: Region code for API (e.g., 130 for Tokyo)
LOCATION=tokyo
AREA_CODE=130

# API Version
# Options: "v2" or "v3"
API_VERSION=v3

# FFmpeg Recording Parameters
# Log level: quiet, fatal, error, warning, info, verbose, debug
FFMPEG_LOGLEVEL=warning

# Reconnection settings (in seconds)
FFMPEG_RECONNECT_DELAY_MAX=600

# Timeout for HTTP connections (in seconds)
FFMPEG_RW_TIMEOUT=900000000

# Audio codec settings
FFMPEG_AUDIO_CODEC=aac
FFMPEG_AUDIO_BITRATE=96k
FFMPEG_AUDIO_SAMPLE_RATE=22050

# Additional ffmpeg options (optional)
# Format: space-separated options (e.g., "-reconnect 1 -reconnect_at_eof 0")
FFMPEG_EXTRA_OPTIONS=-reconnect 1 -reconnect_at_eof 0 -reconnect_streamed 1 -live_start_index -2 -http_persistent 0

# Radiko-specific ffmpeg options (for live and time-free recording)
# These options are used in recorder_radiko.py and ProgramFormatter for Radiko programs
# Default: -loglevel warning -vn -acodec copy (plus reconnection options)
RADIKO_FFMPEG_OPTS=-loglevel warning -y -reconnect 1 -reconnect_at_eof 0 -reconnect_streamed 1 -reconnect_delay_max 600 -user_agent "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/117.0.0.0 Safari/537.36"

# NHK-specific ffmpeg options (for NHK らじる★らじる recording)
# These options are used in recorder_nhk.py and ProgramFormatter for NHK programs
# Default: -loglevel warning -vn -acodec copy (plus reconnection options)
NHK_FFMPEG_OPTS=-loglevel warning -y -reconnect 1 -reconnect_at_eof 0 -reconnect_streamed 1 -reconnect_delay_max 600 -user_agent "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/117.0.0.0 Safari/537.36"

# Radiko Area ID (e.g., JP13 for Tokyo)
RADIKO_AREA_ID=JP13

# NHK Area ID (e.g., JP13 for Tokyo, 130 for area code)
NHK_AREA_ID=JP13
